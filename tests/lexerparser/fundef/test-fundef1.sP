# functions defined inside functions
def int outer():
        def store bool inner(int i, bool j, mutex m22):
                if (1): 3 else if (2): 4 else 5;;
                return 0;
        return inner;