# a lambda with all statement types

(quack -> quack) it =  lambda quack ():
    1 + 3 / 2 * 5 + -2
    a = 7
    int b = 4
    def store int myFunc (int y): return y;
    while (a > 2):
        a = a - 1;
    if (a < 2):
        print("hello")
    else
        if (a > 0): 
            print("good");
        print("too small");
    return ;
