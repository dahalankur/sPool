# a recursive function that uses automatic memoization
def store int fibonacci(int n):
    if (n <= 1):
        return n
    else
        return fibonacci(n-1) + fibonacci(n-2); # this semi-colon closes the if
    ; # closes the def